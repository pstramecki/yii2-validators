parameters:
    level: max
    paths:
        - src
        - tests

    bootstrapFiles:
        - tests/bootstrap.php

    ignoreErrors:
        - "#Method [a-zA-Z0-9\\_\\\\:\\(\\)]+ return type has no value type specified in iterable type array.#"
